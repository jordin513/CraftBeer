package com.nidroj.craftbeer.data

import junit.framework.TestCase
import org.junit.Test


/**
 * I have not implemented these tests yet but I created functions for where
 * I would add tests.
 *
 */
class BeerPagingSourceTest : TestCase(){
    /**
     * Validates the loading function of the BeerPagingSource.
     * Ensures that it is properly paginating the data
     *
     * @param pagingSource Automatically loads pages of data supplied by PunkAPI.
     */
    @Test
    fun validateBeerPagingSourceLoad(pagingSource: BeerPagingSource) {}
}